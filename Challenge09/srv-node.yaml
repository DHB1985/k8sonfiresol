apiVersion: v1
kind: Service
metadata:
  name: nodeserver
  labels:
    app: nodeserver
    tier: backend
spec:
  type: NodePort
  ports: 
    - name: service-http
      port: 8080
      targetPort: node-server
  selector:
    app: nodeserver
    tier: backend